import { useSession } from "next-auth/react";

const Home =()=>{
    
    //const {setAuthenticated}=useContext(ConfigContext);
    return(
        <div>
            
        </div>
    )
}

export default Home;

Home.auth=true;